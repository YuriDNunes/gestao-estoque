import { createTheme } from "@mui/material/styles";

const theme = createTheme({
  palette: {
    primary: {
      main: "#1e3a5f",
      contrastText: "#fff",
    },
    background: {
      default: "#f0f4f8",
      paper: "#fff",
    },
  },
  typography: {},
  shape: {},
  components: {},
});

export default theme;
